import flixel.FlxSprite;

class Item extends FlxSprite
{
	public var RESTORE = 0;

	public function new(x:Float, y:Float)
	{
		super(x, y);
	}

	override function update(elapsed:Float)
	{
		super.update(elapsed);
	}
}
